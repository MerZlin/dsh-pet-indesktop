# Phase 4：DLC 更新中心

本目录存放 DLC 独立更新协议和后续实现说明。当前协议为 [`PLUGIN-UPDATE-PROTOCOL.md`](PLUGIN-UPDATE-PROTOCOL.md)。

计划覆盖：

- DLC catalog；
- staging；
- SHA-256；
- 签名；
- active 版本；
- 原子激活；
- 启动自检；
- 自动回滚；
- Core 与 DLC 分离更新。

当前 Core 自动更新文档仍位于 `docs/ONLINE-UPDATE.md`，不属于本目录的 DLC 协议。
