# Phase 2：Core 插件运行时

本目录预留 Core 插件运行时相关设计和实现说明。

计划内容：

- `PluginRegistry`；
- `PluginContext`；
- `CoreEventBus`；
- capability 检查；
- 插件配置命名空间；
- 官方 in-process 插件；
- 插件故障隔离和诊断；
- content provider 注册。

进入本阶段前，应先完成 `../plugin-phase-01-foundation/` 的 manifest、资源 provider 和兼容检查基础。

当前状态：阶段预留，尚未代表代码已经实现。
