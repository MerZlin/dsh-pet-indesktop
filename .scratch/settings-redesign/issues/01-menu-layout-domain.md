# Menu layout domain

Status: In progress

Implement the versioned default tree, validation, normalization, migration diagnostics, capability resolution, and minimal safety fallback through the agreed Menu layout seam.

## Completion

- Focused tests were observed red then green for each vertical slice.
- `modern-default-v1` is the independent expected fixture.
- Invalid layouts cannot remove all settings or exit paths.
